# Frontend configuration (copy to `.env` or `.env.local`).

# Base URL for the ClipForge API. Defaults to "/api/v1" and is proxied to the
# backend during local dev (see vite.config.ts).
VITE_API_URL=/api/v1

# Dev-server proxy target for /api and /health.
VITE_PROXY_TARGET=http://localhost:8000
