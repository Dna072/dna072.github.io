# Base URL the SPA calls for the RenderFlow API.
# Leave unset in dev to use the Vite proxy (see vite.config.ts) which forwards
# /api and /health to http://localhost:8000. Set explicitly for a production
# build served from a different origin than the API.
VITE_API_BASE_URL=
