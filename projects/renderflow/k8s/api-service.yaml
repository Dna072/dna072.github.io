apiVersion: v1
kind: Service
metadata:
  name: renderflow-api
  labels:
    app.kubernetes.io/name: renderflow
    app.kubernetes.io/component: api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: renderflow
    app.kubernetes.io/component: api
  ports:
    - port: 8000
      targetPort: http
      name: http
